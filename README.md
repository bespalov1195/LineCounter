# LineCounter

``` make
Usage: lineCounter [path] <.extension, ...>
   or: lineCounter [path]
   or: lineCounter <.extension, ...>

Specify: the path to working directory with extension(s)\n \
         or the path to working directory without extension(s), \n \
         or only extension(s).";
```
